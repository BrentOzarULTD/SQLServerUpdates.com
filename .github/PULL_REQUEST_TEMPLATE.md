## What SQL Server build does this add or change?

- Version:
- Update (CU/SP/GDR):
- Build number:
- Release date (YYYY/MM/DD):
- Microsoft KB / release-notes link:

## Checklist

- [ ] I edited the data file(s) under `data/` (not generated HTML).
- [ ] If this is now the newest update for the version, I also updated the
      `home_table` entry in `data/versions.json`.
- [ ] `python3 validate.py` passes locally (optional — CI will also run it).

<sub>The CI checks (validate → build → verify) run automatically on this PR.</sub>
